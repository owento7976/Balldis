package com.example.myapplicationballdis

import android.content.Intent
import android.os.Bundle
import android.widget.Button
import androidx.appcompat.app.AppCompatActivity
import androidx.navigation.ui.AppBarConfiguration
import com.example.myapplicationballdis.databinding.ActivityOfertadoresBinding


class Ofertadores : AppCompatActivity() {

    private var appBarConfiguration: AppBarConfiguration?
        get() = null
        set(value) = TODO()
    private lateinit var binding: ActivityOfertadoresBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }

}

